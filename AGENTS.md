# AGENTS.md

## 适用范围与术语

本文档约束参与 wish-creator 开发的 **Coding Agent**。

- **Coding Agent**：读取、修改和审查本仓库代码的开发助手。
- **许愿池 Agent**：产品内与用户澄清愿望、生成和修改应用的创作 Agent。

两者属于不同领域。讨论实现和协作方式时使用“Coding Agent”；讨论产品行为时使用“许愿池 Agent”。

## 必读规范

按任务类型读取对应文档，再开始设计或实现：

- 所有代码实现、缺陷修复、重构和工程配置任务：[`docs/development-guidelines.md`](docs/development-guidelines.md)
- 涉及产品概念、业务边界、用户旅程、交互或文案的任务：[`docs/product-guidelines.md`](docs/product-guidelines.md)
- 新增或修改用户可观察功能的任务：[`docs/prototype-workflow.md`](docs/prototype-workflow.md)

同时涉及多类任务时，相关文档都要读取。若下层文档与本文件冲突，以本文件为准。

## 必须遵守

1. **先验证事实**：作出技术判断前检查真实代码、类型、依赖、配置或文档，不用合理猜测代替验证。
2. **先理解目标**：先确认要解决的问题、期望结果和关键约束，不把用户提出的实现方式当成唯一方案。
3. **控制确认成本**：低风险且容易回退的选择可以说明假设后继续；只有不同选择会明显影响产品行为、架构、成本或不可逆数据时，才等待用户确认。
4. **区分事实与提案**：明确区分已验证事实、基于事实的判断和尚未实现的建议，不把设计设想描述成现有能力。
5. **原型先行**：新增或修改任何用户可观察功能时，必须先更新 `frontend/src/prototype/` 中的配套原型，使其反映目标流程、界面、状态和文案，再开始编写或修改正式产品代码。需求尚未明确时只维护原型框架，不猜测具体产品行为。正式实现范围变化时也要先同步原型。纯内部重构、工程配置或不改变用户可观察行为的修改不要求虚构原型。
6. **测试先行**：原型反映目标行为后，功能与缺陷修复遵循开发规范中的测试驱动流程；测试失败时不得宣告完成。
7. **完成质量闭环**：功能完成后读取并应用 [`.agents/skills/wish-creator-code-quality-review/SKILL.md`](.agents/skills/wish-creator-code-quality-review/SKILL.md)，修复确认的问题，并通过规定的质量检查。
8. **使用设计技能**：进行原型设计或 UI 设计时，必须使用本项目由 `Leonxlnx/taste-skill` 安装到 [`.agents/skills/`](.agents/skills/) 的相关 Skill，并根据具体任务选择最匹配的设计 Skill。

## 沟通偏好

- 先直接回答当前问题，再补充结论所需的证据和细节。
- 使用自然、具体的语言，避免抽象名词堆叠、口号式总结和不必要的中英文混排。
- 不迎合预设结论；同意时说明理由，不同时直接指出证据、风险和更合适的选择。
- 新证据改变结论时，说明改变了什么、为什么改变，以及旧结论中哪些部分不再成立。
- 维护当前有效结论和待解决问题，不重复背景，不无说明地重新引入已否定的方案。
- 需要用户决策时，明确要决定什么，以及不同选择的实际影响。
